// Create a string saying "You have received this message because you have been chosen to open an important vault. Here is the secret combination."

const message=`You have received this message because you have been chosen to open an important vault. Here is the secret combination`;

// assign 3 variables using a unique arithmetic operator(+-*/). Each must equal one of three codes in the combination. 
const firstNumber=8+2
const secondNumber=4*10
const thirdNumber=35+4



// Create a popup dialog box displaying the vault codes and the text.

alert(`${message} ${firstNumber} ${secondNumber} ${thirdNumber}`);